<Window x:Class="RPMExamPodgot.LoginWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="268" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Авторизация" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Логин:"/>
            <TextBox x:Name="LoginBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Пароль:"/>
            <PasswordBox x:Name="PasswordBox"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Войти" Height="32"
                Click="Login_Click" Margin="0,0,0,8"/>

        <Button Grid.Row="4" Content="Войти как гость"
                Height="32" Click="Guest_Click"/>
    </Grid>
</Window>