<Window x:Class="RPMExamPodgot.ProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Товары" Height="450" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="Grid" AutoGenerateColumns="False"
                  CanUserAddRows="True" CanUserDeleteRows="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"      Binding="{Binding Id}"       IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"     Width="*"/>
                <DataGridTextColumn Header="Цена"    Binding="{Binding Price}"     Width="120"/>
                <DataGridTextColumn Header="Кол-во"  Binding="{Binding Quantity}"  Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveBtn" Content="Сохранить" Width="120" Height="32"
                    Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Закрыть" Width="100" Height="32" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>