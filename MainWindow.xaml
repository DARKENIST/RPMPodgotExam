<Window x:Class="RPMExamPodgot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Главная" Height="420" Width="560"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="GreetingText" FontSize="20" FontWeight="Bold"/>

        <TextBlock x:Name="TimeText" Grid.Row="1" FontSize="14"
                   Foreground="Gray" Margin="0,5,0,20"/>

        <StackPanel Grid.Row="2">
            <Button Content="Просмотр товаров" Height="36" Margin="0,0,0,8"
                    Click="OpenProducts_Click"/>
            <Button x:Name="AdminButton" Content="Управление товарами (админ)"
                    Height="36" Click="OpenAdminProducts_Click"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Выйти" Height="32" Click="Logout_Click"/>
    </Grid>
</Window>
